# Laufzeit-DB (persistente Daten, nie ins Repo)
captly.db
captly.db-*
*.sqlite

# Geheimer ASR-Key (Proxy-Config) — nie ins Repo
config.php

# Hugging-Face-Token (lokal für Showcase-Bildgenerierung, nie ins Repo)
.hf_token

# Node
node_modules/

# OS / Editor
.DS_Store

# Versehentliches Shell-Artefakt
av.getInt16*

# Lokale Preview-Hilfe (Claude)
.claude/
